# huggingface.yaml
# Official Hugging Face Spaces configuration
# Documentation: https://huggingface.co/docs/hub/spaces-config-reference

title: Agent4 Multi-LLM Orchestrator
emoji: 🤖
colorFrom: purple
colorTo: blue
sdk: docker
app_port: 7860
pinned: false
license: mit
duplicated_from: null
tags:
  - llm
  - orchestration
  - multi-agent
  - api
  - typescript
  - docker

# Additional metadata (for documentation purposes only)
# These fields are not processed by Hugging Face Spaces but provide
# useful information for developers and users

# Environment variables required for production deployment:
# - NODE_ENV=production
# - PORT=7860
# - HF_TOKEN (Hugging Face API token)
# - MISTRAL_API_KEY (Mistral AI API key)
# - DEEPSEEK_API_KEY (DeepSeek API key)
# - OPENROUTER_API_KEY (OpenRouter API key)
# - CODESTRAL_API_KEY (Codestral API key)
# - ALIBABA_QWEN_API_KEY (Alibaba Qwen API key)
# - KIMI_API_KEY (Kimi API key)
# - DEFAULT_LLM_PROVIDER=huggingface
# - FALLBACK_ORDER=huggingface,mistral,deepseek,openrouter,codestral
# - LOG_LEVEL=info
# - CORS_ORIGIN=* (set to specific domain in production)
# - RATE_LIMIT_WINDOW_MS=900000
# - RATE_LIMIT_MAX_REQUESTS=100

# Configure these secrets in Space Settings > Repository secrets
